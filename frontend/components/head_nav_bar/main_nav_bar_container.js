import { connect } from "react-redux";
import NavBar from "./main_nav_bar";

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(mapStateToProps, mapDispatchToProps)(NavBar);
